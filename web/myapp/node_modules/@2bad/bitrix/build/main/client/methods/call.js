"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const qs_1 = require("qs");
const isArray_1 = __importDefault(require("../../utils/isArray"));
exports.handlePayload = (payload) => {
    if (payload.error) {
        throw new Error(`[call] failed to get the resource: ${payload.error}.`);
    }
    if (payload.result && payload.result.result_error) {
        const resultErrors = payload.result.result_error;
        const errors = isArray_1.default(resultErrors) ? resultErrors : Object.values(resultErrors);
        if (errors.length > 0) {
            throw new Error(`[batch] failed to process. Received errors in ${errors.length} commands:\n${errors.join('\n')}`);
        }
    }
    return payload;
};
exports.default = ({ get }) => {
    const call = (method, params) => get(method, { query: qs_1.stringify(params) })
        .then(({ body }) => exports.handlePayload(body));
    return call;
};
//# sourceMappingURL=call.js.map