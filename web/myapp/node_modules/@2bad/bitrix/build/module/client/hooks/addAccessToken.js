export default (accessToken) => (options) => {
    if (!accessToken)
        return;
    if (!options.path)
        return;
    const hasQuery = options.path.includes('?');
    options.path = `${options.path}${hasQuery ? '&' : '?'}access_token=${accessToken}`;
};
//# sourceMappingURL=addAccessToken.js.map